document.addEventListener("DOMContentLoaded", function () {
  const form = document.getElementById("info-form");
  form.addEventListener("submit", () => {
    // Optional JS enhancements, not mandatory
  });
});
